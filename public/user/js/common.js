// changes the text on the page add_new_shelter
$(document).ready(function() {
    if ($('.response-status').length) {
        console.log(true);
        $(".form-new-shelter").css("display", "none");
    }
});